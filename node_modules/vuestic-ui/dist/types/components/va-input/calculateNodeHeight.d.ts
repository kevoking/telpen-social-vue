export default function calculateNodeHeight(uiTextNode: any, useCache?: boolean, minRows?: any, maxRows?: any): {
    height: string;
    minHeight: string;
    maxHeight: string;
    overflowY: string | undefined;
};
